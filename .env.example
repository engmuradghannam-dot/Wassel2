# Used by docker-compose.yml. Copy this file to .env and change the values
# before running in anything beyond local/dev.

DB_PASSWORD=murad_erp_secure_2026
JWT_SECRET=change-me-to-a-long-random-string
